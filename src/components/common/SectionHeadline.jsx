import React from "react";

function SectionHeadline({ text }) {
  return (
    <div className="d-flex justify-content-center summary-headline mb-3 mt-3">
      {text}
    </div>
  );
}

export default SectionHeadline;
